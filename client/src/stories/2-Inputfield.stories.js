import React from "react";
import Input from "../components/Inputs/Input";

export default {
  title: "Input"
};

export const Default = () => (
  <Input placeholder="my bad.. what was your name again?" />
);
